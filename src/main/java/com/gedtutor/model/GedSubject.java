package com.gedtutor.model;

/**
 * @deprecated Replaced by the {@link Subject} JPA entity so that admins
 * can add, rename, and deactivate subjects at runtime. This enum is kept
 * only as a placeholder — safe to delete manually from your project.
 */
@Deprecated(forRemoval = true)
public enum GedSubject {
    // No values. Kept as an empty placeholder; references were migrated to Subject.
}
